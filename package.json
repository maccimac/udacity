{
    "scripts": {
        "frontend:install": "cd reactnd-contact-complete && npm install",
        "backend:install": "cd reactnd-contacts-server && npm install",
        "frontend:build": "cd reactnd-contact-complete && npm run build",
        "backend:build": "cd reactnd-contacts-server && npm run build",
        "frontend:deploy": "cd reactnd-contact-complete && npm run build && npm run deploy"
    },
    "dependencies": {
        "yarn": "^1.22.19"
    }
}
